interface:
  display_name: "Agentrium"
  short_description: "Repo-specific patterns and workflows for agentrium"
  default_prompt: "Use the agentrium repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true